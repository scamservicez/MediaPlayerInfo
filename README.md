# 🎵 WindowsMediaPlayerController

Нативная Java-библиотека для управления мультимедиа в Windows через **WinRT MediaSession API**.  
Позволяет получать информацию о текущем воспроизводимом треке, управлять воспроизведением (Play, Pause, Next, Previous, Stop), а также получать обложку, длительность и прогресс.

---

## ⚙️ Возможности

- 🔍 Получение списка активных мультимедиа-сессий (Spotify, Chrome, Edge, YouTube Music и т.д.)
- 🎧 Получение информации о текущем треке:
  - Название
  - Исполнитель
  - Обложка (artwork)
  - Позиция и длительность
  - Состояние (воспроизведение / пауза)
- 🎮 Управление:
  - ▶️ Play / ⏸ Pause
  - ⏭ Next
  - ⏮ Previous
  - ⏹ Stop
---

## 🪟 Совместимость

| Компонент | Версия / Требование |
|------------|---------------------|
| ОС | Windows 10 / 11 |
| JDK | Java 17–23 |
| Архитектура | x64 |
| Нативная библиотека | WinRT (C++/WinRT, Media Control API) |

---

## 📦 Установка

1. Скопируйте проект или добавьте `.java` и `mediaplayer.dll` в свой проект.  
2. Поместите `mediaplayer.dll` в `resources/` (она автоматически загрузится во время работы).  
3. Убедитесь, что приложение запускается на Windows 10+ с поддержкой WinRT.

---

## 💻 Пример использования 

```java
public class Example {
    public static void main(String[] args) {
        WindowsMediaPlayerController controller = new WindowsMediaPlayerController();

        var info = controller.getCurrentTrackInfo();
        if (info != null) {
            System.out.println("Трек: " + info.getTitle());
            System.out.println("Исполнитель: " + info.getArtist());
            System.out.println("Позиция: " + info.getPositionFormatted());
        }

        controller.next(); // Следующий трек
    }
}
